<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.homeis.user.model.mapper.UserMapper">
	<select id="login" parameterType="user" resultType="user">
		select id, name, email, address, birth, point, job, married 
		from user
		where id=#{id} and password=#{password}
	</select>
	
	<insert id="register" parameterType="user">
		insert into user
		(id, password, name, email, address, birth, point, job, married)
		values
		(#{id}, #{password}, #{name}, #{email}, #{address}, #{birth}, #{point}, #{job}, #{married})		
	</insert>
	
	<select id="idExist" parameterType="string" resultType="user">
		select id, name, email, address, birth, point, job, married 
		from user
		where id=#{userId}
	</select>
	
	<update id="updateUserInfo" parameterType="user">
		update user set password=#{password}, name=#{name}, email=#{email}, address=#{address}, birth=#{birth}, point=#{point}, job=#{job}, married=#{married}
		where id=#{id};
	</update>
	
	<delete id="deleteUser" parameterType="string">
		delete from user
		where id=#{userId}
	</delete>
	
	<!--
	<insert id="insertLoginTime" parameterType="string">
		insert into login_history
		(member_id, login_time)
		values
		(#{id}, current_timestamp())
	</insert>
	
	<update id="insertLogoutTime" parameterType="string">
		update login_history as lh1
		join (
		    SELECT member_id, MAX(login_time) AS max_login_time
		    FROM login_history
		    WHERE member_id = #{id}
		    GROUP BY member_id
		) AS lh2 ON lh1.member_id = lh2.member_id AND lh1.login_time = lh2.max_login_time
		SET lh1.logout_time = current_timestamp();
		
	</update> -->
</mapper>